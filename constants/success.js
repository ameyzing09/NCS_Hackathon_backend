const { SUCCESS } = require('./httpStatusCode');

module.exports = {
    QRCODE_GENERATED_SUCCESSFULLY: {
        code: SUCCESS,
        message: 'QR code generated successfully'
    },
    URL_SHORTEN_SUCCESSFULLY: {
        code: SUCCESS,
        message: 'URL shortened successfully'
    }
}